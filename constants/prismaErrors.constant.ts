/**
 * Prisma error codes for common database operations
 */
export const PRISMA_ERROR_CODES = {
  /**
   * Record not found error - occurs when trying to update, delete or query a record that doesn't exist
   */
  RECORD_NOT_FOUND: "P2025",
};
